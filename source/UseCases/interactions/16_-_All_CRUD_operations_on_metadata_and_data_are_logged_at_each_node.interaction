title 16 - All CRUD operations on metadata and data are logged at each node

actor User
User -> app_client
app_client -> m_crud: create (token, dataPackage)
note right
See Use Case 4
end note
m_crud -> m_crud: log (create, creationDate, ID)
m_crud -> m_crud: ack
